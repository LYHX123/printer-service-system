# Copy this file to ".env" on the server.
# Used by docker-compose for variable interpolation (db credentials, domain).

# --- PostgreSQL ---
POSTGRES_USER=printerapp
POSTGRES_PASSWORD=changeme-strong-password
POSTGRES_DB=printer_service

# --- Domain (used by Caddy for automatic HTTPS) ---
DOMAIN=printerservice.yourcompany.com
